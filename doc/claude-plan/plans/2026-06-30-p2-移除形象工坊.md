# P2 移除形象工坊 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 从 Claude360 Copilot（原 Kun）完整移除「形象工坊」——左下角小鸟吉祥物、主会话 iKun cameo 彩蛋、回合完成庆祝彩蛋、UI 插件系统、彩蛋设置分区、预装 iKun 插件、Retroma 配色——同时**完整保留** `AgentKun`（子 Agent 头像）、`KunStateFigure`/`KunHeroStage`（工作态/空会话 logo）、`AnimatedWorkLogo`（进行中泳动 logo）。

**Architecture:** 「先剥离保留组件对 UI 插件 store 的引用 → 再删彩蛋组件与渲染点 → 再删设置页与运行时 store → 再删主进程链路 → 最后删样式/i18n/资源并重写测试」。Task 1–7 是阶段性切割，允许出现文档明确列出的白名单 typecheck 报错；Task 10 必须 `npm run typecheck` 0 错误。这是删除/重构任务，以**回归验证**（typecheck + build + 保留能力测试 + 启动冒烟）替代 TDD。

**Tech Stack:** Electron 34 + React 19 + TypeScript + Zustand + Vite（electron-vite）+ Vitest。

---

## 执行约定（务必先读）

1. **Git 提交规范**：本项目规范是**不主动执行 git 提交/分支**。各 Task 末尾的「提交」为**可选**，由用户决定是否执行；执行者默认只改文件、跑验证，不自动 commit。
2. **验证命令**（项目根 `/root/app/claude360agent/claude360-Copilot`）：
   - 类型检查：`npm run typecheck`（= `tsc --noEmit -p tsconfig.web.json && tsc --noEmit -p tsconfig.node.json`）
   - Lint：`npm run lint`
   - 测试：`npm run test`（= `vitest run`）
   - 构建：`npm run build`
3. **行号会漂移**：计划中给出的行号基于当前快照，仅供定位。执行前先 `Read` 目标文件，用**符号名/代码块**精确匹配后再改。
4. **保留清单（任何 Task 都不得删除）**：
   - `src/renderer/src/components/subagents/AgentKun.tsx` 及其用图 `kun_clip/search/laptop/magic/cheer/headset/wrench/rest.png`
   - `src/renderer/src/components/chat/KunHeroStage.tsx`（空会话/唤醒舞台 logo）
   - `AnimatedWorkLogo.tsx` 中的 `KunStateFigure`、`AnimatedWorkLogo`、`useWorkLogoSwimMode`、`WORK_LOGO_SWIM_MODE*`、`KunStateFigureKind`、`KUN_STATE_FIGURES` 及用图 `kun_bird/surf/greet/sleep/sit.png`
   - 应用图标 `kun.png/kun_mac.png/kun_tray.png`
   - locales 的 `working/workingSprint/workingDive/workingSurf`
   - CSS `.ds-work-logo*`、`.ds-kun-state*`、`.ds-agent-kun*`、`.ds-runtime-wake-*`
5. **危险操作确认**：执行 `rm`、`rm -rf`、批量替换或删除目录前，必须先用 `rg`/`ls` 输出影响清单，并按仓库 AGENTS 约定向用户确认。确认格式：
   ```text
   ⚠️ 危险操作检测！
   操作类型：[删除文件/删除目录/批量替换]
   影响范围：[列出文件或目录清单]
   风险评估：[潜在后果与回滚方式]

   请确认是否继续？[需要明确的"是"、"确认"、"继续"]
   ```

---

## File Structure（改动总览）

**改造（保留组件，剥离彩蛋/插件依赖）：**
- `src/renderer/src/components/chat/AnimatedWorkLogo.tsx` — 删彩蛋，留 KunStateFigure/AnimatedWorkLogo/swim
- `src/renderer/src/components/chat/MessageTimeline.tsx` — LiveTurnProgressRow 去 iKun/pluginLabel
- `src/renderer/src/components/chat/Sidebar.tsx` — 删 SidebarMascot
- `src/renderer/src/components/Workbench.tsx` — 删 cameo/celebration 渲染与初始化
- `src/renderer/src/components/SettingsView.tsx`、`src/renderer/src/components/SettingsSidebar.tsx`、`src/renderer/src/components/settings-sections.tsx`、`src/renderer/src/store/chat-store-types.ts`、`src/renderer/src/components/use-settings-gui-update.ts` — 去 `easterEgg` 枚举/注册
- `src/main/ipc/register-app-ipc-handlers.ts`、`src/main/ipc/app-ipc-schemas.ts`、`src/preload/index.ts`、`src/shared/kun-gui-api.ts`、`src/main/index.ts` — 去 ui-plugin IPC 与 boot 调用
- `src/renderer/src/styles/base-shell.css`、`src/renderer/src/styles/surfaces-write.css` — 删彩蛋样式
- `src/renderer/src/locales/{zh,en}/common.json`、`src/renderer/src/locales/{zh,en}/settings.json` — 删彩蛋文案

**删除 / 重写：**
- `src/renderer/src/components/settings-section-easter-egg.tsx` + `.test.ts`
- `src/renderer/src/store/ui-plugin-store.ts`
- `src/renderer/src/lib/ui-mode.ts`、`src/renderer/src/lib/ikun-mode.ts` + `ikun-mode.test.ts`
- `src/main/services/ui-plugin-service.ts` + `.test.ts`
- `src/main/ui-plugin-bundled.ts`
- `src/shared/ui-plugin.ts` + `.test.ts`
- `src/renderer/src/components/chat/AnimatedWorkLogo.test.ts`（不删除，重写：删 iKun/UI plugin/cameo/celebration 断言，保留工作 logo 与状态形象回归）
- `examples/ui-plugins/`（整目录）、`docs/UI_PLUGINS.md`
- 资源图：`src/asset/img/ikun.png、ikun_run.png、ikun_boba.png、ikun_wave.png、ikun_sleep.png、ikun_stand.png、ikun-ui-plugin.gif、ikun-ui-plugin.mp4`

---

## Task 1: 改造 AnimatedWorkLogo.tsx（剥离彩蛋，保留工作 logo）

**Files:**
- Modify: `src/renderer/src/components/chat/AnimatedWorkLogo.tsx`（整文件替换为下方内容）

- [x] **Step 1: 用以下完整内容替换整个文件**

```tsx
import type { ReactElement } from 'react'
import { useEffect, useState } from 'react'
import kunLogo from '../../../../asset/img/kun_bird.png'
import kunSurfFigure from '../../../../asset/img/kun_surf.png'
import kunGreetFigure from '../../../../asset/img/kun_greet.png'
import kunSleepFigure from '../../../../asset/img/kun_sleep.png'
import kunSitFigure from '../../../../asset/img/kun_sit.png'

export type WorkLogoSwimMode = 'propel' | 'sprint' | 'dive' | 'surf'

export const WORK_LOGO_SWIM_MODES: readonly WorkLogoSwimMode[] = [
  'propel',
  'sprint',
  'dive',
  'surf'
]

export const WORK_LOGO_SWIM_MODE_LABEL_KEYS: Record<WorkLogoSwimMode, string> = {
  propel: 'working',
  sprint: 'workingSprint',
  dive: 'workingDive',
  surf: 'workingSurf'
}

const WORK_LOGO_SWIM_MODE_INTERVAL_MS = 4200

export function useWorkLogoSwimMode(active: boolean): WorkLogoSwimMode {
  // 起点随机,避免每次都从「推进中」开始;之后按顺序轮播
  const [modeIndex, setModeIndex] = useState(() =>
    Math.floor(Math.random() * WORK_LOGO_SWIM_MODES.length)
  )

  useEffect(() => {
    if (!active) return
    const interval = window.setInterval(() => {
      setModeIndex((current) => (current + 1) % WORK_LOGO_SWIM_MODES.length)
    }, WORK_LOGO_SWIM_MODE_INTERVAL_MS)
    return () => window.clearInterval(interval)
  }, [active])

  return WORK_LOGO_SWIM_MODES[modeIndex] ?? 'propel'
}

export type KunStateFigureKind = 'greet' | 'sleep' | 'sit'

const KUN_STATE_FIGURES: Record<KunStateFigureKind, string> = {
  greet: kunGreetFigure,
  sleep: kunSleepFigure,
  sit: kunSitFigure
}

/** 静态场景里的形象:打招呼(欢迎)、睡觉(运行时待唤醒)、坐着(空状态) */
export function KunStateFigure({
  kind,
  className = ''
}: {
  kind: KunStateFigureKind
  className?: string
}): ReactElement {
  return (
    <span
      className={['ds-kun-state', `ds-kun-state-${kind}`, className].filter(Boolean).join(' ')}
      aria-hidden="true"
    >
      <img
        className="ds-kun-state-figure"
        src={KUN_STATE_FIGURES[kind]}
        alt=""
        draggable={false}
        decoding="async"
      />
    </span>
  )
}

export function AnimatedWorkLogo({
  active = false,
  className = '',
  mode,
  phase = 'lead',
  size = 'sm'
}: {
  active?: boolean
  className?: string
  mode?: WorkLogoSwimMode
  phase?: 'lead' | 'trail'
  size?: 'sm' | 'md'
}): ReactElement {
  const rotatedSwimMode = useWorkLogoSwimMode(active && mode === undefined)
  const swimMode = mode ?? rotatedSwimMode
  const figureSrc = swimMode === 'surf' ? kunSurfFigure : kunLogo

  return (
    <span
      className={[
        'ds-work-logo',
        `ds-work-logo-${size}`,
        `ds-work-logo-phase-${phase}`,
        `ds-work-logo-mode-${swimMode}`,
        active ? 'is-active' : '',
        className
      ]
        .filter(Boolean)
        .join(' ')}
      aria-hidden="true"
    >
      <span className="ds-work-logo-gust" />
      <span className="ds-work-logo-current" />
      <span className="ds-work-logo-swell" />
      <span className="ds-work-logo-wave ds-work-logo-wave-back" />
      <span className="ds-work-logo-ripple" />
      <span className="ds-work-logo-wave ds-work-logo-wave-front" />
      <span className="ds-work-logo-breaker" />
      <span className="ds-work-logo-wake" />
      <span className="ds-work-logo-foam" />
      <span className="ds-work-logo-crest" />
      <span className="ds-work-logo-splash" />
      <span className="ds-work-logo-spray" />
      <span className="ds-work-logo-bubbles" />
      <img className="ds-work-logo-echo" src={figureSrc} alt="" draggable={false} decoding="async" />
      <span className="ds-work-logo-track">
        <span className="ds-work-logo-body">
          <img className="ds-work-logo-image" src={figureSrc} alt="" draggable={false} decoding="async" />
        </span>
      </span>
    </span>
  )
}
```

> 关键变化：删除 `useUiPluginFigure`/`UiPluginFigureSlot` 与全部 `ikun*` 图 import；删除 `KUN_STATE_IKUN_FIGURES`、`UI_PLUGIN_*_SLOTS`、cameo 全块、celebration 全块、`SidebarMascot`、iKun work-logo 变体；`KunStateFigure` 去掉第二张 `ds-ikun-state-figure` 图与 store hook；`AnimatedWorkLogo` 去掉 `ikunVariant` 形参与 `ds-ikun-logo` 块。`useRef` 不再被引用，已从 import 移除。

- [x] **Step 2: 类型检查**

Run: `npm run typecheck`
Expected: 此处会出现「`AnimatedWorkLogo.tsx` 不再导出 `SidebarMascot`/`IkunCameoLayer`/`KunCelebrationLayer`/`useIkunWorkLogoVariant`/`IKUN_WORK_LOGO_VARIANT_LABEL_KEYS`」相关的报错，来自 `MessageTimeline.tsx`、`Sidebar.tsx`、`Workbench.tsx`、`AnimatedWorkLogo.test.ts` —— **这是阶段性白名单报错**，将在 Task 2–4、Task 9 消除。先确认报错范围仅限这几个文件；若出现其它文件，先停下修正计划。

- [x] **Step 3（可选）提交**

```bash
git add src/renderer/src/components/chat/AnimatedWorkLogo.tsx
git commit -m "refactor(mascot): strip avatar-workshop cameos from AnimatedWorkLogo"
```

---

## Task 2: 改造 MessageTimeline.tsx（LiveTurnProgressRow 去 iKun/插件文案）

**Files:**
- Modify: `src/renderer/src/components/chat/MessageTimeline.tsx`（import 块 + `LiveTurnProgressRow`）

- [x] **Step 1: 替换 AnimatedWorkLogo 的 import 块**

把当前（约 14-22 行）：

```tsx
import {
  AnimatedWorkLogo,
  IKUN_WORK_LOGO_VARIANT_LABEL_KEYS,
  WORK_LOGO_SWIM_MODE_LABEL_KEYS,
  useIkunWorkLogoVariant,
  useWorkLogoSwimMode
} from './AnimatedWorkLogo'
import type { UiPluginLabelKey } from '@shared/ui-plugin'
import { useUiPluginWorkLabel } from '../../store/ui-plugin-store'
```

替换为：

```tsx
import {
  AnimatedWorkLogo,
  WORK_LOGO_SWIM_MODE_LABEL_KEYS,
  useWorkLogoSwimMode
} from './AnimatedWorkLogo'
```

- [x] **Step 2: 替换 `LiveTurnProgressRow` 整个函数**

把当前 `LiveTurnProgressRow`（约 662-690 行）替换为：

```tsx
function LiveTurnProgressRow({ hasActiveGoal }: { hasActiveGoal: boolean }): ReactElement {
  const { t } = useTranslation('common')
  const swimMode = useWorkLogoSwimMode(true)
  const swimLabelKey = WORK_LOGO_SWIM_MODE_LABEL_KEYS[swimMode]
  const label = t(swimLabelKey)

  return (
    <div className={liveTurnProgressClass(hasActiveGoal)}>
      <span className="ds-work-logo-slot ds-work-logo-slot-sm mr-0.5">
        <AnimatedWorkLogo active mode={swimMode} phase="trail" size="sm" />
      </span>
      <span className="ds-shiny-text">{label}</span>
    </div>
  )
}
```

> 变化：删除 `useIkunWorkLogoVariant`、`ikunModeOn`（`data-ikun-mode` 读取）、`useUiPluginWorkLabel`/`pluginLabel`；`label` 直接取默认泳姿文案；`AnimatedWorkLogo` 去掉 `ikunVariant` 形参；不再解构 `i18n`。

- [x] **Step 3: 类型检查**

Run: `npm run typecheck`
Expected: `MessageTimeline.tsx` 相关报错消失（剩余报错仍来自 Sidebar/Workbench/测试）。

- [x] **Step 4（可选）提交**

```bash
git add src/renderer/src/components/chat/MessageTimeline.tsx
git commit -m "refactor(timeline): drop iKun variant + ui-plugin label from progress row"
```

---

## Task 3: 改造 Sidebar.tsx（删左下角小鸟入口）

**Files:**
- Modify: `src/renderer/src/components/chat/Sidebar.tsx`（删 import + footer 渲染块）

- [x] **Step 1: 删除 SidebarMascot 的 import**

删除该行（约 27 行）：

```tsx
import { SidebarMascot } from './AnimatedWorkLogo'
```

- [x] **Step 2: 删除 footer 里的小鸟渲染块**

把当前（约 143-148 行）：

```tsx
          <div className="flex min-h-[42px] items-center justify-center gap-2.5 pb-1">
            {!focusModeEnabled ? (
              <span className="flex h-[46px] w-[56px] shrink-0 items-center justify-center">
                <SidebarMascot />
              </span>
            ) : null}
            <FocusModeToggle
```

替换为：

```tsx
          <div className="flex min-h-[42px] items-center justify-center gap-2.5 pb-1">
            <FocusModeToggle
```

> 说明：仅删除 `SidebarMascot` 及其外层 `<span>` 包裹，保留 `FocusModeToggle` 及其全部 props 不变。左下角腾出的空间将在后续里程碑 P3（「我的」入口）使用，本计划不新增内容。

- [x] **Step 3: 类型检查**

Run: `npm run typecheck`
Expected: `Sidebar.tsx` 相关报错消失。

- [x] **Step 4（可选）提交**

```bash
git add src/renderer/src/components/chat/Sidebar.tsx
git commit -m "refactor(sidebar): remove mascot from footer"
```

---

## Task 4: 改造 Workbench.tsx（删彩蛋渲染与 UI 插件初始化）

**Files:**
- Modify: `src/renderer/src/components/Workbench.tsx`

- [x] **Step 1: 删除两条 import**

删除（约 45 行）：

```tsx
import { IkunCameoLayer, KunCelebrationLayer } from './chat/AnimatedWorkLogo'
```

删除（约 97 行）：

```tsx
import { useUiModeCameosEnabled, useUiPluginStore } from '../store/ui-plugin-store'
```

> 执行时先 `Read` 确认这两行的实际 import 形态（可能与其它 import 合并），仅移除 `IkunCameoLayer`、`KunCelebrationLayer`、`useUiModeCameosEnabled`、`useUiPluginStore` 四个符号；若该 import 行还引入了其它仍在用的符号，保留它们。

- [x] **Step 2: 删除 hook 调用与初始化 effect**

删除这两行（约 537-538 行，注意 selector 是 `s.initUiPlugins`）：

```tsx
  const initUiPlugins = useUiPluginStore((s) => s.initUiPlugins)
  const uiModeCameosEnabled = useUiModeCameosEnabled()
```

删除其对应的初始化 effect（约 835-838 行，effect 体为 `void initUiPlugins()`）：

```tsx
  useEffect(() => {
    void initUiPlugins()
  }, [initUiPlugins])
```

> ⚠️ **风险提示**：该 effect **紧邻**约 842 行的 `document.documentElement.setAttribute('data-focus-mode', ...)` effect（**保留，不可删**）。执行时 `Read` 文件，**仅**删除 effect 体为 `void initUiPlugins()` 的那一个，确认 `initUiPlugins`/`uiModeCameosEnabled` 删后在文件内无其它引用，且 `data-focus-mode` effect 完好。

- [x] **Step 3: 删除两个渲染分支**

删除这两行（约 2768-2769 行）：

```tsx
        {uiModeCameosEnabled && !focusModeEnabled ? <IkunCameoLayer /> : null}
        {!focusModeEnabled ? <KunCelebrationLayer active={busy} suppressed={Boolean(error)} /> : null}
```

> 执行时 `Read` 确认这两个渲染分支的实际形态（`KunCelebrationLayer` 真实 props 为 `active={busy} suppressed={Boolean(error)}`），整段删除这两行。

- [x] **Step 4: 类型检查**

Run: `npm run typecheck`
Expected: `Workbench.tsx` 相关报错消失。此时仅剩 `AnimatedWorkLogo.test.ts`（Task 9 重写）与尚未触及的 easterEgg/ui-plugin 链路（Task 5–7）的报错。

- [x] **Step 5（可选）提交**

```bash
git add src/renderer/src/components/Workbench.tsx
git commit -m "refactor(workbench): remove cameo/celebration layers and ui-plugin init"
```

---

## Task 5: 删除彩蛋设置页与其注册点

**Files:**
- Delete: `src/renderer/src/components/settings-section-easter-egg.tsx`、`src/renderer/src/components/settings-section-easter-egg.test.ts`
- Modify: `src/renderer/src/components/SettingsView.tsx`、`src/renderer/src/components/SettingsSidebar.tsx`、`src/renderer/src/components/settings-sections.tsx`、`src/renderer/src/store/chat-store-types.ts`、`src/renderer/src/components/use-settings-gui-update.ts`

- [x] **Step 1: 删除设置页组件与其测试**

执行前先按「危险操作确认」输出以下文件清单，并获得用户明确确认。

```bash
rm src/renderer/src/components/settings-section-easter-egg.tsx
rm src/renderer/src/components/settings-section-easter-egg.test.ts
```

- [x] **Step 2: `src/renderer/src/components/settings-sections.tsx` 删导出**

删除该行（约 13 行）：

```tsx
export { EasterEggSettingsSection } from './settings-section-easter-egg'
```

- [x] **Step 3: `src/renderer/src/components/SettingsView.tsx` 删 lazy 引入、类型成员、路由分支、渲染**

依次删除：
- lazy 定义（约 87-89 行）：`const EasterEggSettingsSection = lazy(() => import('./settings-section-easter-egg')...)`
- `SettingsCategory` 联合类型里的 `| 'easterEgg'`（约 125 行）
- 路由映射分支（约 374-375 行）：`if (settingsSection === 'easterEgg') { setCategory('easterEgg') }`
- 条件判断里的 `settingsSection === 'easterEgg' ||`（约 401 行）
- `Exclude<...>` 列表里的 `'easterEgg'`（约 410 行）
- 渲染分支（约 1173 行）：`{category === 'easterEgg' ? <EasterEggSettingsSection ... /> : null}`

> 执行时 `Read` 文件，按 `easterEgg` 字符串逐处定位精确删除；删后 `EasterEggSettingsSection` 在文件内无引用。

- [x] **Step 4: `src/renderer/src/components/SettingsSidebar.tsx` 删类型成员与导航按钮**

- `SettingsCategory` 联合类型里删 `| 'easterEgg'`（约 4 行）
- 删除「形象工坊」侧栏按钮整块（约 130-137 行，`catCls('easterEgg')` + `onClick` + `t('easterEgg')`）

- [x] **Step 5: `src/renderer/src/store/chat-store-types.ts` 删枚举成员**

`SettingsRouteSection` 联合类型里删 `| 'easterEgg'`（约 95 行）。

- [x] **Step 6: `src/renderer/src/components/use-settings-gui-update.ts` 删枚举成员**

`category` 联合类型里删 `'easterEgg'`（约 17 行）。

- [x] **Step 7: 类型检查**

Run: `npm run typecheck`
Expected: 所有 `easterEgg` 相关报错消失（剩余报错来自 ui-plugin 链路与 store，Task 6–7 处理）。

- [x] **Step 8（可选）提交**

```bash
git add -A src/renderer/src/components/SettingsView.tsx src/renderer/src/components/SettingsSidebar.tsx src/renderer/src/components/settings-sections.tsx src/renderer/src/store/chat-store-types.ts src/renderer/src/components/use-settings-gui-update.ts
git commit -m "refactor(settings): remove easter-egg (avatar workshop) section"
```

---

## Task 6: 删除渲染层 UI 插件运行时（store / ui-mode / ikun-mode）

**Files:**
- Delete: `src/renderer/src/store/ui-plugin-store.ts`、`src/renderer/src/lib/ui-mode.ts`、`src/renderer/src/lib/ikun-mode.ts`、`src/renderer/src/lib/ikun-mode.test.ts`

- [x] **Step 1: 确认无残留引用**

Run: `rg -n "ui-plugin-store|from '.*ui-mode'|ikun-mode|useUiPlugin|useUiModeCameosEnabled" src/renderer`
Expected: 仅命中将被删除的这 4 个文件自身（Task 1–5 已清理 AnimatedWorkLogo/MessageTimeline/Workbench/设置页的全部引用）。若命中其它**保留文件**，先回到对应 Task 清理。

- [x] **Step 2: 删除文件**

执行前先按「危险操作确认」输出以下文件清单，并获得用户明确确认。

```bash
rm src/renderer/src/store/ui-plugin-store.ts
rm src/renderer/src/lib/ui-mode.ts
rm src/renderer/src/lib/ikun-mode.ts
rm src/renderer/src/lib/ikun-mode.test.ts
```

- [x] **Step 3: 类型检查**

Run: `npm run typecheck`
Expected: 渲染层 ui-plugin/ui-mode 报错消失；可能仍剩主进程 `kun-gui-api.ts`/preload 对 `@shared/ui-plugin` 的引用（Task 7 处理）。

- [x] **Step 4（可选）提交**

```bash
git add -A
git commit -m "refactor(renderer): remove ui-plugin store and ui-mode/ikun-mode"
```

---

## Task 7: 删除主进程 UI 插件链路（IPC / service / bundled / 契约）

**Files:**
- Modify: `src/main/ipc/register-app-ipc-handlers.ts`、`src/main/ipc/app-ipc-schemas.ts`、`src/preload/index.ts`、`src/shared/kun-gui-api.ts`
- Delete: `src/main/services/ui-plugin-service.ts`、`ui-plugin-service.test.ts`、`src/main/ui-plugin-bundled.ts`、`src/shared/ui-plugin.ts`、`ui-plugin.test.ts`

- [x] **Step 1: `register-app-ipc-handlers.ts` 删 import 与 4 个 handler**

- 删 import：`uiPluginIdPayloadSchema`（约 76 行）、`installUiPluginFromDirectory/listUiPlugins/loadUiPluginFigures/removeUiPlugin`（约 147-151 行）、`ensureBundledUiPlugins`（约 152 行）
- 删 4 个 handler 注册：`ui-plugin:list`、`ui-plugin:install`、`ui-plugin:remove`、`ui-plugin:load`（约 922-958 行）

> 注意：`ensureBundledUiPlugins` 在 `ui-plugin:install`（约 924 行）与 `ui-plugin:load`（约 956 行）两个 handler **内部**被调用，删除这两个 handler 即一并移除其调用。`Read` 文件按 `ui-plugin`/`UiPlugin`/`ensureBundledUiPlugins` 逐处定位删除。

- [x] **Step 2: `app-ipc-schemas.ts` 删 schema**

删除 `uiPluginIdPayloadSchema` 定义（约 1831 行起的整段）。`Read` 确认无其它 schema 引用它。

- [x] **Step 3: `preload/index.ts` 删 4 个桥接**

删除 `listUiPlugins`、`installUiPlugin`、`removeUiPlugin`、`loadUiPlugin` 四个方法（约 83-90 行）。

- [x] **Step 4: `kun-gui-api.ts` 删类型与接口成员**

- 删 import（约 88-91 行）：`UiPluginListItem/UiPluginManifestV1/UiPluginRuntimeFigures from './ui-plugin'`
- 删类型 `UiPluginListIpcResult`、`UiPluginInstallIpcResult`、`UiPluginLoadIpcResult`（约 175-181 行）
- 删 `kunGui` 接口成员 `listUiPlugins/installUiPlugin/removeUiPlugin/loadUiPlugin`（约 375-378 行）

- [x] **Step 5: 全局核实 `ensureBundledUiPlugins` 已随 handler 删除**

> **修正**：`ensureBundledUiPlugins` **不在** `src/main/index.ts`（已核实 index.ts 无任何 bundled 引用）。它仅在 `register-app-ipc-handlers.ts` 的 `ui-plugin:install`（约 924 行）与 `ui-plugin:load`（约 956 行）handler 内被调用，已随 Step 1 删除这两个 handler 一并移除。

本步需在删除文件前确认所有运行时引用都已被清掉，避免只查 `src/main/index.ts` 得出误判：

Run: `rg -n "ensureBundledUiPlugins|ui-plugin-bundled" src/main src/preload src/shared src/renderer`
Expected: 无命中。

- [x] **Step 6: 删除文件**

执行前先按「危险操作确认」输出以下文件清单，并获得用户明确确认。

```bash
rm src/main/services/ui-plugin-service.ts
rm src/main/services/ui-plugin-service.test.ts
rm src/main/ui-plugin-bundled.ts
rm src/shared/ui-plugin.ts
rm src/shared/ui-plugin.test.ts
```

- [x] **Step 7: 全局确认无残留引用**

Run: `rg -n "ui-plugin|UiPlugin|ensureBundledUiPlugins" src/main src/preload src/shared src/renderer`
Expected: 无命中（全部清理完毕）。

- [x] **Step 8: 类型检查**

Run: `npm run typecheck`
Expected: 通过（0 错误）。

- [x] **Step 9（可选）提交**

```bash
git add -A
git commit -m "refactor(main): remove ui-plugin IPC, service, bundled seed and contract"
```

---

## Task 8: 删除彩蛋样式

**Files:**
- Modify: `src/renderer/src/styles/base-shell.css`、`surfaces-write.css`

- [x] **Step 1: `base-shell.css` 删彩蛋样式块**

删除以下选择器的全部规则（`Read` 后按选择器名定位）：
- `.ds-ikun-cameo*`（cameo 全部）
- `.ds-kun-celebration*`、`.ds-kun-confetti*`（庆祝彩带全部）
- `.ds-sidebar-mascot`
- `.ds-ikun-logo*`、`.ds-ikun-figure`
- `.ds-ikun-state-figure`
- 全部 `[data-ikun-mode='on']` 选择器（含其下的 `.ds-kun-state*` 切换、工作 logo 染色等）
- 全部 `[data-retroma-mode='on']` 选择器（Retroma 配色，随工坊一并删）

**保留（不可删）**：`.ds-work-logo*`、`.ds-kun-state` / `.ds-kun-state-figure` / `.ds-kun-state-{greet,sleep,sit}`（默认形象）、`.ds-agent-kun*`、`.ds-runtime-wake-*`。

- [x] **Step 2: `surfaces-write.css` 删 2 条规则**

删除两条 `[data-ikun-mode='on'] .ds-stage-surface::before` 规则（约 21、28 行）。

- [x] **Step 3: 确认无残留**

Run: `rg -n "ikun|data-ikun-mode|data-retroma-mode|kun-celebration|kun-confetti|sidebar-mascot|ds-ikun" src/renderer/src/styles`
Expected: 无命中。

- [x] **Step 4（可选）提交**

```bash
git add -A src/renderer/src/styles
git commit -m "style: remove avatar-workshop (cameo/celebration/ikun/retroma) css"
```

---

## Task 9: 删除 i18n 文案、资源图、示例与文档，重写保留测试

**Files:**
- Modify: `src/renderer/src/locales/{zh,en}/common.json`、`src/renderer/src/locales/{zh,en}/settings.json`
- Delete: 资源图、`examples/ui-plugins/`、`docs/UI_PLUGINS.md`
- Modify: `src/renderer/src/components/chat/AnimatedWorkLogo.test.ts`

- [x] **Step 1: 删 settings.json 文案（zh 与 en 各一份）**

删除这些 key：`easterEgg`、`easterEggSection`、`uiModeWorkshopTitle`、`uiModeWorkshopDesc`、`uiModeDefaultTitle`、`uiModeDefaultSubtitle`、`uiPaletteRetromaOn`、`uiPaletteRetromaOff`、`uiPluginInstall`、`uiPluginActivate`、`uiPluginActive`、`uiPluginRemove`、`uiPluginEmpty`、`uiPluginInstallFailed`、`uiPluginDocsHint`。

- [x] **Step 2: 删 common.json 文案（zh 与 en 各一份）**

删除这些 key：`ikunMode`、`ikunDribbling`、`ikunFastBreak`、`ikunBobaTime`。
**保留**：`working`、`workingSprint`、`workingDive`、`workingSurf`（工作进度文案，仍被引用）。

- [x] **Step 3: 校验 JSON 合法**

Run: `node -e "for (const f of ['zh/common','en/common','zh/settings','en/settings']) JSON.parse(require('fs').readFileSync('src/renderer/src/locales/'+f+'.json','utf8')); console.log('json ok')"`
Expected: 输出 `json ok`（无尾逗号等语法错误）。

- [x] **Step 4: 删资源图与示例、文档**

执行前先按「危险操作确认」输出待删资源、目录和文档清单，并获得用户明确确认。

```bash
rm src/asset/img/ikun.png src/asset/img/ikun_run.png src/asset/img/ikun_boba.png src/asset/img/ikun_wave.png src/asset/img/ikun_sleep.png src/asset/img/ikun_stand.png
rm src/asset/img/ikun-ui-plugin.gif src/asset/img/ikun-ui-plugin.mp4
rm -rf examples/ui-plugins
rm docs/UI_PLUGINS.md
```

> 删前确认：`rg -n "ikun\.png|ikun_run|ikun_boba|ikun_wave|ikun_sleep|ikun_stand|ikun-ui-plugin" src` 应无命中（Task 1 已移除 AnimatedWorkLogo 的 import）。

- [x] **Step 5: 重写 `AnimatedWorkLogo.test.ts`，保留回归保护**

不要删除整个测试文件。把它改成只覆盖改造后仍必须保留的能力：
- `kun_bird.png` 默认工作 logo 资源仍存在，尺寸断言保留。
- `AnimatedWorkLogo` 仍渲染 `.ds-work-logo*` 分层结构，且不再包含 `.ds-ikun-logo` / `.ds-ikun-figure`。
- `KunStateFigure` 仍支持 `greet/sleep/sit` 三种 kind，且不再包含 `.ds-ikun-state-figure`。
- `WORK_LOGO_SWIM_MODES` 与 `WORK_LOGO_SWIM_MODE_LABEL_KEYS` 覆盖 `propel/sprint/dive/surf`，文案 key 仍为 `working/workingSprint/workingDive/workingSurf`。
- `kun_greet.png`、`kun_sleep.png`、`kun_surf.png`、`kun_sit.png` 状态图资源尺寸断言保留。
- 删除所有 `@shared/ui-plugin`、`useUiPluginStore`、`SidebarMascot`、`IkunCameo*`、`KunCelebration*`、`IKUN_WORK_LOGO*`、iKun 资源尺寸断言。

Run: `npm run test -- AnimatedWorkLogo`
Expected: PASS，且测试只覆盖保留项与“iKun DOM 不再出现”的负向断言。

- [x] **Step 6（可选）提交**

```bash
git add -A
git commit -m "chore: remove avatar-workshop i18n/assets and keep logo tests"
```

---

## Task 10: 全量验证

**Files:** 无（仅运行验证）

- [x] **Step 1: 类型检查**

Run: `npm run typecheck`
Expected: PASS（0 错误）。

- [x] **Step 2: Lint**

Run: `npm run lint`
Expected: 无新增错误（若有未使用 import/变量残留，回到对应 Task 清理）。

- [x] **Step 3: 单元测试**

Run: `npm run test`
Expected: PASS。`AnimatedWorkLogo.test.ts` 已重写为保留项回归测试，其余测试不回归。

- [x] **Step 4: 全局确认形象工坊痕迹清零**

Run: `rg -n -i "ui-plugin|UiPlugin|ikun|easter|cameo|celebration|sidebar-mascot|retroma" src --glob '!*.test.ts'`
Expected: 仅可能命中保留项的良性词（如 `AgentKun`、`KunStateFigure` 等含 "kun" 但非工坊）；**不得**再有 `ui-plugin`/`ikun`/`easterEgg`/`cameo`/`celebration` 的功能性引用。逐条核对命中项确属保留范围。

- [x] **Step 5: 构建**

Run: `npm run build`
Expected: PASS（electron-vite 构建成功）。

- [x] **Step 6: 启动冒烟（人工）**

Run: `npm run dev`
人工确认：① 左下角不再出现小鸟吉祥物，仅 FocusModeToggle 居中；② 主会话区不再出现 iKun 出没/庆祝彩蛋；③ 设置侧栏不再有「形象工坊」分区；④ **保留项正常**：进行中回合的泳动 logo（AnimatedWorkLogo）、空会话欢迎舞台（KunHeroStage/KunStateFigure）、子 Agent 卡片头像（AgentKun）均正常显示。

- [x] **Step 7（可选）提交**

```bash
git add -A
git commit -m "test: verify avatar-workshop fully removed; runtime/build green"
```

---

## Self-Review（计划自检）

**Spec 覆盖**：对照 P2 依赖图——左下角小鸟（Task 1+3）、主会话 cameo（Task 1+4）、庆祝彩蛋（Task 1+4）、UI 插件系统（Task 6+7）、彩蛋设置页（Task 5）、预装 iKun（Task 7）、Retroma 配色（Task 8 样式 + Task 6 store）、i18n/资源（Task 9）—— 全部有对应 Task。✓

**保留项保护**：`AgentKun`、`KunHeroStage`、`KunStateFigure`、`AnimatedWorkLogo`、`useWorkLogoSwimMode` 及其用图/CSS/文案在 Task 1 与「保留清单」中明确保留，Task 9 重写 `AnimatedWorkLogo.test.ts` 保留自动化回归，Task 10 Step 6 再人工验证。✓

**类型一致性**：`AnimatedWorkLogo` 改造后去掉 `ikunVariant` 形参，其唯一调用方 `MessageTimeline.LiveTurnProgressRow`（Task 2）同步去掉该实参；`WORK_LOGO_SWIM_MODE_LABEL_KEYS`/`useWorkLogoSwimMode` 签名不变，保留方引用一致。✓

**顺序安全**：先剥离引用（Task 1-4）再删 store（Task 6）再删主进程契约（Task 7）；Task 1–7 后的 typecheck 允许且仅允许文档列出的阶段性白名单报错，Task 10 必须 0 错误。✓

**无占位**：改造类文件给完整新代码（Task 1-3）；删除类给精确文件/符号清单 + `rg` 确认命中。✓
